<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yuber Anthony Ticona Incacutipa | Developer Portfolio</title>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;600;700&family=Space+Grotesk:wght@300;400;600;700&family=Orbitron:wght@400;700;900&family=Roboto+Mono:wght@300;400;500;700&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- AOS - Animate On Scroll -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <!-- Particles.js -->
    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
    
    <!-- Three.js -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    
    <!-- Typed.js -->
    <script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.12"></script>
    
    <!-- GSAP para animaciones avanzadas -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    
    <!-- Firebase -->
    <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-storage-compat.js"></script>
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #00ff88;
            --secondary: #00d4ff;
            --accent: #ff0080;
            --purple: #b794f6;
            --bg-dark: #0a0e27;
            --bg-darker: #050810;
            --bg-light: #1a1f3a;
            --card-bg: rgba(10, 14, 39, 0.95);
            --text-primary: #ffffff;
            --text-secondary: #94a3b8;
            --code-green: #00ff88;
            --neon-blue: #00d4ff;
            --neon-pink: #ff0080;
            --gradient-1: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --gradient-2: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            --gradient-3: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Space Grotesk', sans-serif;
            background: var(--bg-darker);
            color: var(--text-primary);
            overflow-x: hidden;
            position: relative;
        }

        /* ==================== LOADING SCREEN ==================== */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--bg-darker);
            z-index: 10000;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            transition: opacity 0.5s, visibility 0.5s;
        }

        .loading-screen.hidden {
            opacity: 0;
            visibility: hidden;
        }

        .loader {
            width: 120px;
            height: 120px;
            border: 3px solid rgba(0, 255, 136, 0.1);
            border-top: 3px solid var(--primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .loading-text {
            margin-top: 30px;
            font-family: 'JetBrains Mono', monospace;
            color: var(--primary);
            font-size: 1.2em;
            animation: pulse 1.5s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        /* ==================== BACKGROUND EFFECTS ==================== */
        #particles-js {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: 0;
        }

        .gradient-orbs {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: 1;
            overflow: hidden;
            pointer-events: none;
        }

        .orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(100px);
            opacity: 0.4;
            animation: float 20s ease-in-out infinite;
        }

        .orb1 {
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, #667eea, transparent);
            top: -200px;
            left: -200px;
            animation-delay: 0s;
        }

        .orb2 {
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, #f093fb, transparent);
            bottom: -150px;
            right: -150px;
            animation-delay: -5s;
        }

        .orb3 {
            width: 450px;
            height: 450px;
            background: radial-gradient(circle, #00d4ff, transparent);
            top: 40%;
            right: 20%;
            animation-delay: -10s;
        }

        @keyframes float {
            0%, 100% {
                transform: translate(0, 0) scale(1);
            }
            33% {
                transform: translate(100px, -100px) scale(1.1);
            }
            66% {
                transform: translate(-100px, 100px) scale(0.9);
            }
        }

        /* ==================== HEADER & NAVIGATION ==================== */
        .main-header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            background: rgba(5, 8, 16, 0.8);
            backdrop-filter: blur(20px) saturate(180%);
            border-bottom: 1px solid rgba(0, 255, 136, 0.1);
            transition: all 0.3s ease;
        }

        .main-header.scrolled {
            background: rgba(5, 8, 16, 0.95);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
        }

        nav {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.5em;
            font-weight: 900;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            cursor: pointer;
            transition: transform 0.3s ease;
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .nav-links {
            display: flex;
            gap: 40px;
            list-style: none;
        }

        .nav-links a {
            color: var(--text-secondary);
            text-decoration: none;
            font-weight: 600;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.95em;
            position: relative;
            transition: color 0.3s ease;
        }

        .nav-links a::before {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--primary);
            transition: width 0.3s ease;
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        .nav-links a:hover::before {
            width: 100%;
        }

        .theme-toggle {
            background: rgba(0, 255, 136, 0.1);
            border: 2px solid rgba(0, 255, 136, 0.3);
            color: var(--primary);
            padding: 10px 20px;
            border-radius: 10px;
            cursor: pointer;
            font-family: 'JetBrains Mono', monospace;
            transition: all 0.3s ease;
        }

        .theme-toggle:hover {
            background: rgba(0, 255, 136, 0.2);
            transform: translateY(-2px);
        }

        /* ==================== HERO SECTION ==================== */
        .hero-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            padding: 100px 40px 60px;
            z-index: 10;
        }

        .hero-container {
            max-width: 1400px;
            width: 100%;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
        }

        .hero-content {
            z-index: 10;
        }

        .hero-tag {
            display: inline-block;
            padding: 8px 20px;
            background: rgba(0, 255, 136, 0.1);
            border: 1px solid rgba(0, 255, 136, 0.3);
            border-radius: 50px;
            color: var(--primary);
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.9em;
            margin-bottom: 20px;
        }

        .hero-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 4em;
            font-weight: 900;
            line-height: 1.2;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #ffffff, var(--primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-subtitle {
            font-size: 1.8em;
            color: var(--text-secondary);
            margin-bottom: 30px;
            font-weight: 300;
        }

        .hero-description {
            font-size: 1.2em;
            color: var(--text-secondary);
            line-height: 1.8;
            margin-bottom: 40px;
        }

        .hero-cta {
            display: flex;
            gap: 20px;
        }

        .btn-primary, .btn-secondary {
            padding: 18px 40px;
            border-radius: 12px;
            font-family: 'JetBrains Mono', monospace;
            font-weight: 600;
            font-size: 1.05em;
            cursor: pointer;
            transition: all 0.3s ease;
            border: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: var(--bg-dark);
            box-shadow: 0 10px 30px rgba(0, 255, 136, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(0, 255, 136, 0.5);
        }

        .btn-secondary {
            background: transparent;
            color: var(--primary);
            border: 2px solid var(--primary);
        }

        .btn-secondary:hover {
            background: rgba(0, 255, 136, 0.1);
            transform: translateY(-3px);
        }

        /* ==================== 3D SCENE ==================== */
        .hero-visual {
            position: relative;
            height: 500px;
        }

        #canvas-3d {
            width: 100%;
            height: 100%;
            border-radius: 20px;
        }

        /* ==================== STATS SECTION ==================== */
        .stats-section {
            padding: 80px 40px;
            position: relative;
            z-index: 10;
        }

        .stats-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 30px;
        }

        .stat-card {
            background: rgba(10, 14, 39, 0.8);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(0, 255, 136, 0.2);
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 136, 0.1), transparent);
            transition: left 0.5s ease;
        }

        .stat-card:hover::before {
            left: 100%;
        }

        .stat-card:hover {
            transform: translateY(-10px);
            border-color: var(--primary);
            box-shadow: 0 20px 60px rgba(0, 255, 136, 0.3);
        }

        .stat-icon {
            font-size: 3em;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 20px;
        }

        .stat-number {
            font-family: 'Orbitron', sans-serif;
            font-size: 3em;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 10px;
        }

        .stat-label {
            font-family: 'JetBrains Mono', monospace;
            color: var(--text-secondary);
            font-size: 1em;
        }

        /* ==================== SKILLS SECTION ==================== */
        .skills-section {
            padding: 80px 40px;
            position: relative;
            z-index: 10;
        }

        .section-header {
            text-align: center;
            margin-bottom: 60px;
        }

        .section-tag {
            display: inline-block;
            padding: 8px 20px;
            background: rgba(0, 255, 136, 0.1);
            border: 1px solid rgba(0, 255, 136, 0.3);
            border-radius: 50px;
            color: var(--primary);
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.9em;
            margin-bottom: 20px;
        }

        .section-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 3em;
            font-weight: 900;
            margin-bottom: 20px;
        }

        .section-description {
            font-size: 1.2em;
            color: var(--text-secondary);
            max-width: 700px;
            margin: 0 auto;
        }

        .skills-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 40px;
        }

        .skill-category {
            background: rgba(10, 14, 39, 0.8);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(0, 255, 136, 0.2);
            border-radius: 20px;
            padding: 40px;
        }

        .skill-category h3 {
            font-family: 'JetBrains Mono', monospace;
            font-size: 1.5em;
            color: var(--primary);
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .skill-item {
            margin-bottom: 25px;
        }

        .skill-name {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 1em;
        }

        .skill-bar {
            height: 10px;
            background: rgba(0, 255, 136, 0.1);
            border-radius: 10px;
            overflow: hidden;
            position: relative;
        }

        .skill-progress {
            height: 100%;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
            border-radius: 10px;
            transition: width 1.5s ease;
            position: relative;
            overflow: hidden;
        }

        .skill-progress::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            animation: shimmer 2s infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        /* ==================== COURSE INFO ==================== */
        .course-section {
            padding: 80px 40px;
            position: relative;
            z-index: 10;
        }

        .course-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
        }

        .course-card {
            background: rgba(10, 14, 39, 0.8);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(0, 255, 136, 0.2);
            border-radius: 20px;
            padding: 40px;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .course-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, var(--primary), var(--secondary), var(--accent));
            transform: scaleX(0);
            transition: transform 0.4s ease;
        }

        .course-card:hover::before {
            transform: scaleX(1);
        }

        .course-card:hover {
            transform: translateY(-10px);
            border-color: var(--primary);
            box-shadow: 0 20px 60px rgba(0, 255, 136, 0.3);
        }

        .course-card h3 {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.5em;
            color: var(--primary);
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .course-card p, .course-card ul {
            color: var(--text-secondary);
            line-height: 1.8;
        }

        .course-card ul {
            list-style: none;
            padding: 0;
            margin-top: 15px;
        }

        .course-card li {
            padding-left: 25px;
            position: relative;
            margin: 10px 0;
        }

        .course-card li::before {
            content: '▹';
            position: absolute;
            left: 0;
            color: var(--primary);
            font-size: 1.2em;
        }

        /* ==================== TIMELINE ==================== */
        .timeline-section {
            padding: 80px 40px;
            position: relative;
            z-index: 10;
        }

        .timeline-container {
            max-width: 900px;
            margin: 0 auto;
            position: relative;
        }

        .timeline-line {
            position: absolute;
            left: 50%;
            top: 0;
            bottom: 0;
            width: 2px;
            background: linear-gradient(180deg, var(--primary), var(--secondary));
            transform: translateX(-50%);
        }

        .timeline-item {
            position: relative;
            margin-bottom: 60px;
            display: flex;
            align-items: center;
        }

        .timeline-item:nth-child(odd) {
            flex-direction: row-reverse;
        }

        .timeline-content {
            width: 45%;
            background: rgba(10, 14, 39, 0.8);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(0, 255, 136, 0.2);
            border-radius: 20px;
            padding: 30px;
            transition: all 0.4s ease;
        }

        .timeline-content:hover {
            transform: scale(1.05);
            border-color: var(--primary);
            box-shadow: 0 20px 60px rgba(0, 255, 136, 0.3);
        }

        .timeline-dot {
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            width: 20px;
            height: 20px;
            background: var(--primary);
            border: 4px solid var(--bg-darker);
            border-radius: 50%;
            box-shadow: 0 0 20px var(--primary);
        }

        .timeline-date {
            font-family: 'JetBrains Mono', monospace;
            color: var(--primary);
            font-size: 0.9em;
            margin-bottom: 10px;
        }

        .timeline-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.3em;
            margin-bottom: 10px;
        }

        .timeline-description {
            color: var(--text-secondary);
            line-height: 1.6;
        }

        /* ==================== ACTIVITIES SECTION ==================== */
        .activities-section {
            padding: 80px 40px;
            position: relative;
            z-index: 10;
        }

        .activities-container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .add-activity-form {
            background: rgba(10, 14, 39, 0.8);
            backdrop-filter: blur(20px);
            border: 2px solid rgba(0, 255, 136, 0.3);
            border-radius: 20px;
            padding: 40px;
            margin-bottom: 50px;
        }

        .form-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.8em;
            color: var(--primary);
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 10px;
            color: var(--primary);
            font-weight: 600;
            font-family: 'JetBrains Mono', monospace;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 15px;
            background: rgba(0, 255, 136, 0.05);
            border: 2px solid rgba(0, 255, 136, 0.3);
            border-radius: 12px;
            color: var(--text-primary);
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1em;
            transition: all 0.3s ease;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary);
            background: rgba(0, 255, 136, 0.1);
            box-shadow: 0 0 20px rgba(0, 255, 136, 0.2);
        }

        .form-group textarea {
            min-height: 120px;
            resize: vertical;
            font-family: 'JetBrains Mono', monospace;
        }

        .file-input-wrapper {
            position: relative;
        }

        .file-input-wrapper input[type=file] {
            display: none;
        }

        .file-input-label {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            padding: 18px;
            background: linear-gradient(135deg, rgba(0, 255, 136, 0.2), rgba(0, 212, 255, 0.2));
            border: 2px solid rgba(0, 255, 136, 0.4);
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'JetBrains Mono', monospace;
        }

        .file-input-label:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(0, 255, 136, 0.4);
        }

        .selected-files {
            margin-top: 15px;
            padding: 15px;
            background: rgba(0, 255, 136, 0.05);
            border-radius: 12px;
            min-height: 50px;
        }

        .file-tag {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: var(--bg-dark);
            padding: 8px 16px;
            border-radius: 25px;
            margin: 5px;
            font-family: 'JetBrains Mono', monospace;
            font-weight: 600;
            font-size: 0.9em;
        }

        .activities-grid {
            display: grid;
            gap: 30px;
        }

        .activity-card {
            background: rgba(10, 14, 39, 0.8);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(0, 255, 136, 0.2);
            border-radius: 20px;
            padding: 40px;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .activity-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 136, 0.1), transparent);
            transition: left 0.6s ease;
        }

        .activity-card:hover::before {
            left: 100%;
        }

        .activity-card:hover {
            transform: translateY(-5px);
            border-color: var(--primary);
            box-shadow: 0 20px 60px rgba(0, 255, 136, 0.3);
        }

        .activity-header {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px solid rgba(0, 255, 136, 0.2);
        }

        .activity-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.8em;
            color: var(--primary);
            margin-bottom: 10px;
        }

        .activity-date {
            font-family: 'JetBrains Mono', monospace;
            color: var(--text-secondary);
            font-size: 0.95em;
        }

        .activity-description {
            color: var(--text-secondary);
            line-height: 1.8;
            margin-bottom: 20px;
        }

        .pdf-list {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid rgba(0, 255, 136, 0.2);
        }

        .pdf-item {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 12px 20px;
            background: linear-gradient(135deg, rgba(0, 255, 136, 0.2), rgba(0, 212, 255, 0.2));
            border: 1px solid rgba(0, 255, 136, 0.4);
            border-radius: 12px;
            color: white;
            text-decoration: none;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.95em;
            transition: all 0.3s ease;
        }

        .pdf-item:hover {
            transform: translateY(-3px);
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            box-shadow: 0 10px 30px rgba(0, 255, 136, 0.4);
        }

        .delete-btn {
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.8), rgba(220, 38, 38, 0.8));
            border: 2px solid rgba(239, 68, 68, 0.5);
            color: white;
            padding: 10px 20px;
            border-radius: 10px;
            cursor: pointer;
            font-family: 'JetBrains Mono', monospace;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .delete-btn:hover {
            background: linear-gradient(135deg, rgb(239, 68, 68), rgb(220, 38, 38));
            transform: scale(1.05);
            box-shadow: 0 5px 20px rgba(239, 68, 68, 0.5);
        }

        .empty-state {
            text-align: center;
            padding: 80px 20px;
            color: var(--text-secondary);
        }

        .empty-state i {
            font-size: 5em;
            margin-bottom: 25px;
            opacity: 0.3;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* ==================== FOOTER ==================== */
        footer {
            padding: 60px 40px 30px;
            background: rgba(5, 8, 16, 0.95);
            border-top: 1px solid rgba(0, 255, 136, 0.2);
            position: relative;
            z-index: 10;
        }

        .footer-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-about h3 {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.5em;
            color: var(--primary);
            margin-bottom: 20px;
        }

        .footer-about p {
            color: var(--text-secondary);
            line-height: 1.8;
            margin-bottom: 20px;
        }

        .social-links {
            display: flex;
            gap: 15px;
        }

        .social-link {
            width: 45px;
            height: 45px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: rgba(0, 255, 136, 0.1);
            border: 2px solid rgba(0, 255, 136, 0.3);
            border-radius: 50%;
            color: var(--primary);
            transition: all 0.3s ease;
        }

        .social-link:hover {
            background: var(--primary);
            color: var(--bg-dark);
            transform: translateY(-3px);
            box-shadow: 0 5px 20px rgba(0, 255, 136, 0.5);
        }

        .footer-links h4 {
            font-family: 'JetBrains Mono', monospace;
            color: var(--primary);
            margin-bottom: 20px;
        }

        .footer-links ul {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .footer-links a:hover {
            color: var(--primary);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(0, 255, 136, 0.1);
            color: var(--text-secondary);
            font-family: 'JetBrains Mono', monospace;
        }

        /* ==================== RESPONSIVE ==================== */
        @media (max-width: 1024px) {
            .hero-container {
                grid-template-columns: 1fr;
            }

            .stats-container {
                grid-template-columns: repeat(2, 1fr);
            }

            .skills-container {
                grid-template-columns: 1fr;
            }

            .course-container {
                grid-template-columns: 1fr;
            }

            .footer-container {
                grid-template-columns: 1fr 1fr;
            }
        }

        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .hero-title {
                font-size: 2.5em;
            }

            .stats-container {
                grid-template-columns: 1fr;
            }

            .timeline-item {
                flex-direction: column !important;
            }

            .timeline-content {
                width: 100%;
            }

            .timeline-line {
                display: none;
            }
        }

        /* ==================== SCROLLBAR ==================== */
        ::-webkit-scrollbar {
            width: 12px;
        }

        ::-webkit-scrollbar-track {
            background: var(--bg-darker);
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 10px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--primary);
        }
    </style>
</head>
<body>
    <!-- Loading Screen -->
    <div class="loading-screen" id="loadingScreen">
        <div class="loader"></div>
        <div class="loading-text">Cargando portafolio...</div>
    </div>

    <!-- Particles Background -->
    <div id="particles-js"></div>

    <!-- Gradient Orbs -->
    <div class="gradient-orbs">
        <div class="orb orb1"></div>
        <div class="orb orb2"></div>
        <div class="orb orb3"></div>
    </div>

    <!-- Header -->
    <header class="main-header" id="mainHeader">
        <nav>
            <div class="logo">&lt;YAT/&gt;</div>
            <ul class="nav-links">
                <li><a href="#home">Inicio</a></li>
                <li><a href="#skills">Habilidades</a></li>
                <li><a href="#course">Curso</a></li>
                <li><a href="#activities">Actividades</a></li>
                <li><a href="#contact">Contacto</a></li>
            </ul>
            <button class="theme-toggle">
                <i class="fas fa-moon"></i>
            </button>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero-section" id="home">
        <div class="hero-container">
            <div class="hero-content" data-aos="fade-right">
                <div class="hero-tag">
                    <i class="fas fa-code"></i> Developer Portfolio
                </div>
                <h1 class="hero-title">Yuber Anthony Ticona Incacutipa</h1>
                <p class="hero-subtitle" id="typed-subtitle"></p>
                <p class="hero-description">
                    Estudiante apasionado por la programación y el desarrollo de software. 
                    Enfocado en crear soluciones innovadoras mediante código limpio y eficiente.
                </p>
                <div class="hero-cta">
                    <button class="btn-primary" onclick="document.getElementById('activities').scrollIntoView({behavior: 'smooth'})">
                        <i class="fas fa-rocket"></i> Ver Proyectos
                    </button>
                    <button class="btn-secondary" onclick="document.getElementById('contact').scrollIntoView({behavior: 'smooth'})">
                        <i class="fas fa-envelope"></i> Contactar
                    </button>
                </div>
            </div>
            <div class="hero-visual" data-aos="fade-left">
                <canvas id="canvas-3d"></canvas>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats-section">
        <div class="stats-container">
            <div class="stat-card" data-aos="fade-up" data-aos-delay="100">
                <div class="stat-icon"><i class="fas fa-code"></i></div>
                <div class="stat-number" data-count="5000">0</div>
                <div class="stat-label">Líneas de Código</div>
            </div>
            <div class="stat-card" data-aos="fade-up" data-aos-delay="200">
                <div class="stat-icon"><i class="fas fa-project-diagram"></i></div>
                <div class="stat-number" data-count="25">0</div>
                <div class="stat-label">Proyectos</div>
            </div>
            <div class="stat-card" data-aos="fade-up" data-aos-delay="300">
                <div class="stat-icon"><i class="fas fa-brain"></i></div>
                <div class="stat-number" data-count="75">0</div>
                <div class="stat-label">Conceptos Dominados</div>
            </div>
            <div class="stat-card" data-aos="fade-up" data-aos-delay="400">
                <div class="stat-icon"><i class="fas fa-trophy"></i></div>
                <div class="stat-number" data-count="100">0</div>
                <div class="stat-label">% Dedicación</div>
            </div>
        </div>
    </section>

    <!-- Skills Section -->
    <section class="skills-section" id="skills">
        <div class="section-header" data-aos="fade-up">
            <div class="section-tag"><i class="fas fa-cogs"></i> Mis Habilidades</div>
            <h2 class="section-title">Stack Tecnológico</h2>
            <p class="section-description">
                Tecnologías y herramientas que domino en mi camino como desarrollador
            </p>
        </div>
        <div class="skills-container">
            <div class="skill-category" data-aos="fade-right">
                <h3><i class="fas fa-code"></i> Lenguajes de Programación</h3>
                <div class="skill-item">
                    <div class="skill-name">
                        <span>Python</span>
                        <span>85%</span>
                    </div>
                    <div class="skill-bar">
                        <div class="skill-progress" style="width: 0%" data-progress="85"></div>
                    </div>
                </div>
                <div class="skill-item">
                    <div class="skill-name">
                        <span>C++</span>
                        <span>75%</span>
                    </div>
                    <div class="skill-bar">
                        <div class="skill-progress" style="width: 0%" data-progress="75"></div>
                    </div>
                </div>
                <div class="skill-item">
                    <div class="skill-name">
                        <span>Java</span>
                        <span>70%</span>
                    </div>
                    <div class="skill-bar">
                        <div class="skill-progress" style="width: 0%" data-progress="70"></div>
                    </div>
                </div>
                <div class="skill-item">
                    <div class="skill-name">
                        <span>JavaScript</span>
                        <span>80%</span>
                    </div>
                    <div class="skill-bar">
                        <div class="skill-progress" style="width: 0%" data-progress="80"></div>
                    </div>
                </div>
            </div>

            <div class="skill-category" data-aos="fade-left">
                <h3><i class="fas fa-tools"></i> Herramientas & Frameworks</h3>
                <div class="skill-item">
                    <div class="skill-name">
                        <span>Git & GitHub</span>
                        <span>90%</span>
                    </div>
                    <div class="skill-bar">
                        <div class="skill-progress" style="width: 0%" data-progress="90"></div>
                    </div>
                </div>
                <div class="skill-item">
                    <div class="skill-name">
                        <span>VS Code</span>
                        <span>95%</span>
                    </div>
                    <div class="skill-bar">
                        <div class="skill-progress" style="width: 0%" data-progress="95"></div>
                    </div>
                </div>
                <div class="skill-item">
                    <div class="skill-name">
                        <span>Data Structures</span>
                        <span>80%</span>
                    </div>
                    <div class="skill-bar">
                        <div class="skill-progress" style="width: 0%" data-progress="80"></div>
                    </div>
                </div>
                <div class="skill-item">
                    <div class="skill-name">
                        <span>Algorithms</span>
                        <span>75%</span>
                    </div>
                    <div class="skill-bar">
                        <div class="skill-progress" style="width: 0%" data-progress="75"></div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Course Section -->
    <section class="course-section" id="course">
        <div class="section-header" data-aos="fade-up">
            <div class="section-tag"><i class="fas fa-graduation-cap"></i> Lenguaje de Programación I</div>
            <h2 class="section-title">Sobre el Curso</h2>
            <p class="section-description">
                Fundamentos esenciales de programación y desarrollo de software
            </p>
        </div>
        <div class="course-container">
            <div class="course-card" data-aos="flip-left" data-aos-delay="100">
                <h3><i class="fas fa-book-open"></i> Descripción</h3>
                <p>
                    Curso fundamental que introduce los conceptos esenciales de la programación. 
                    Aprenderemos a pensar como programadores y a resolver problemas mediante 
                    algoritmos eficientes y código estructurado.
                </p>
            </div>

            <div class="course-card" data-aos="flip-left" data-aos-delay="200">
                <h3><i class="fas fa-bullseye"></i> Objetivos</h3>
                <ul>
                    <li>Desarrollar pensamiento algorítmico</li>
                    <li>Dominar estructuras de datos</li>
                    <li>Implementar algoritmos eficientes</li>
                    <li>Escribir código limpio y mantenible</li>
                </ul>
            </div>

            <div class="course-card" data-aos="flip-left" data-aos-delay="300">
                <h3><i class="fas fa-code"></i> Temas</h3>
                <ul>
                    <li>Variables y tipos de datos</li>
                    <li>Estructuras de control</li>
                    <li>Funciones y modularidad</li>
                    <li>Arrays y estructuras</li>
                    <li>Algoritmos de ordenamiento</li>
                    <li>Recursividad y complejidad</li>
                </ul>
            </div>

            <div class="course-card" data-aos="flip-left" data-aos-delay="400">
                <h3><i class="fas fa-laptop-code"></i> Tecnologías</h3>
                <p>
                    Python, C++, Java, Git/GitHub, VS Code, debuggers, unit testing frameworks, 
                    y herramientas de análisis de código.
                </p>
            </div>

            <div class="course-card" data-aos="flip-left" data-aos-delay="500">
                <h3><i class="fas fa-rocket"></i> Metodología</h3>
                <p>
                    Aprendizaje basado en proyectos, pair programming, code reviews, 
                    y resolución de problemas mediante desafíos progresivos.
                </p>
            </div>

            <div class="course-card" data-aos="flip-left" data-aos-delay="600">
                <h3><i class="fas fa-award"></i> Competencias</h3>
                <ul>
                    <li>Análisis y diseño algorítmico</li>
                    <li>Debugging avanzado</li>
                    <li>Control de versiones</li>
                    <li>Documentación técnica</li>
                </ul>
            </div>
        </div>
    </section>

    <!-- Timeline Section -->
    <section class="timeline-section">
        <div class="section-header" data-aos="fade-up">
            <div class="section-tag"><i class="fas fa-clock"></i> Mi Progreso</div>
            <h2 class="section-title">Línea de Tiempo</h2>
        </div>
        <div class="timeline-container">
            <div class="timeline-line"></div>
            
            <div class="timeline-item" data-aos="fade-right">
                <div class="timeline-content">
                    <div class="timeline-date">Enero 2026</div>
                    <h3 class="timeline-title">Inicio del Curso</h3>
                    <p class="timeline-description">
                        Comenzamos con los fundamentos de programación, sintaxis básica y primeros algoritmos.
                    </p>
                </div>
                <div class="timeline-dot"></div>
            </div>

            <div class="timeline-item" data-aos="fade-left">
                <div class="timeline-content">
                    <div class="timeline-date">Febrero 2026</div>
                    <h3 class="timeline-title">Estructuras de Control</h3>
                    <p class="timeline-description">
                        Dominio de condicionales, bucles y control de flujo. Primeros proyectos significativos.
                    </p>
                </div>
                <div class="timeline-dot"></div>
            </div>

            <div class="timeline-item" data-aos="fade-right">
                <div class="timeline-content">
                    <div class="timeline-date">Marzo 2026</div>
                    <h3 class="timeline-title">Funciones y Modularidad</h3>
                    <p class="timeline-description">
                        Aprendizaje de funciones, parámetros, alcance de variables y programación modular.
                    </p>
                </div>
                <div class="timeline-dot"></div>
            </div>

            <div class="timeline-item" data-aos="fade-left">
                <div class="timeline-content">
                    <div class="timeline-date">En Progreso</div>
                    <h3 class="timeline-title">Estructuras de Datos</h3>
                    <p class="timeline-description">
                        Estudio de arrays, listas, pilas, colas y sus aplicaciones prácticas.
                    </p>
                </div>
                <div class="timeline-dot"></div>
            </div>
        </div>
    </section>

    <!-- Activities Section -->
    <section class="activities-section" id="activities">
        <div class="section-header" data-aos="fade-up">
            <div class="section-tag"><i class="fas fa-tasks"></i> Mis Proyectos</div>
            <h2 class="section-title">Actividades y Trabajos</h2>
            <p class="section-description">
                Colección de proyectos, prácticas y actividades realizadas durante el curso
            </p>
        </div>

        <div class="activities-container">
            <!-- Add Activity Form -->
            <div class="add-activity-form" data-aos="zoom-in">
                <h3 class="form-title">
                    <i class="fas fa-plus-circle"></i> Agregar Nueva Actividad
                </h3>
                
                <form id="activityForm">
                    <div class="form-group">
                        <label for="activityTitle">
                            <i class="fas fa-heading"></i> Título del Proyecto
                        </label>
                        <input type="text" id="activityTitle" placeholder="Ej: Sistema de Gestión con Estructuras de Datos" required>
                    </div>

                    <div class="form-group">
                        <label for="activityDescription">
                            <i class="fas fa-align-left"></i> Descripción Técnica
                        </label>
                        <textarea id="activityDescription" placeholder="Describe objetivos, tecnologías, algoritmos implementados y resultados..." required></textarea>
                    </div>

                    <div class="form-group">
                        <label>
                            <i class="fas fa-file-pdf"></i> Documentación PDF
                        </label>
                        <div class="file-input-wrapper">
                            <input type="file" id="pdfFiles" accept=".pdf" multiple>
                            <label for="pdfFiles" class="file-input-label">
                                <i class="fas fa-cloud-upload-alt"></i>
                                Seleccionar Archivos PDF
                            </label>
                        </div>
                        <div class="selected-files" id="selectedFiles">
                            <span style="color: var(--text-secondary);">
                                <i class="fas fa-info-circle"></i> No hay archivos seleccionados
                            </span>
                        </div>
                    </div>

                    <button type="submit" class="btn-primary">
                        <i class="fas fa-rocket"></i>
                        Publicar Actividad
                    </button>
                </form>
            </div>

            <!-- Activities Grid -->
            <div class="activities-grid" id="activitiesList">
                <div class="empty-state">
                    <i class="fas fa-folder-open"></i>
                    <h3>Sin Actividades Registradas</h3>
                    <p>Agrega tu primera actividad usando el formulario</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer id="contact">
        <div class="footer-container">
            <div class="footer-about">
                <h3>Yuber Anthony Ticona Incacutipa</h3>
                <p>
                    Estudiante apasionado de Lenguaje de Programación I. Enfocado en desarrollar 
                    habilidades sólidas en programación y crear soluciones innovadoras.
                </p>
                <div class="social-links">
                    <a href="#" class="social-link"><i class="fab fa-github"></i></a>
                    <a href="#" class="social-link"><i class="fab fa-linkedin"></i></a>
                    <a href="#" class="social-link"><i class="fab fa-twitter"></i></a>
                    <a href="#" class="social-link"><i class="fas fa-envelope"></i></a>
                </div>
            </div>

            <div class="footer-links">
                <h4>Navegación</h4>
                <ul>
                    <li><a href="#home">Inicio</a></li>
                    <li><a href="#skills">Habilidades</a></li>
                    <li><a href="#course">Curso</a></li>
                    <li><a href="#activities">Actividades</a></li>
                </ul>
            </div>

            <div class="footer-links">
                <h4>Recursos</h4>
                <ul>
                    <li><a href="#">Documentación</a></li>
                    <li><a href="#">Tutoriales</a></li>
                    <li><a href="#">GitHub Repos</a></li>
                    <li><a href="#">Blog</a></li>
                </ul>
            </div>

            <div class="footer-links">
                <h4>Contacto</h4>
                <ul>
                    <li><a href="#">Email</a></li>
                    <li><a href="#">LinkedIn</a></li>
                    <li><a href="#">Teléfono</a></li>
                </ul>
            </div>
        </div>

        <div class="footer-bottom">
            <p>&copy; 2026 Yuber Anthony Ticona Incacutipa. Todos los derechos reservados.</p>
        </div>
    </footer>

    <!-- AOS Library -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>

    <script>
        // ==========================================
        // LOADING SCREEN
        // ==========================================
        window.addEventListener('load', () => {
            setTimeout(() => {
                document.getElementById('loadingScreen').classList.add('hidden');
            }, 1500);
        });

        // ==========================================
        // FIREBASE CONFIGURATION
        // ==========================================
        const firebaseConfig = {
            apiKey: "AIzaSyBXxvKZ3bH8nF7vNxPqR9YzJ3wKxLmN0pQ",
            authDomain: "portafolio-programacion.firebaseapp.com",
            projectId: "portafolio-programacion",
            storageBucket: "portafolio-programacion.appspot.com",
            messagingSenderId: "123456789012",
            appId: "1:123456789012:web:abcdef1234567890"
        };

        firebase.initializeApp(firebaseConfig);
        const db = firebase.firestore();
        const storage = firebase.storage();

        // ==========================================
        // AOS INITIALIZATION
        // ==========================================
        AOS.init({
            duration: 1000,
            once: true,
            offset: 100
        });

        // ==========================================
        // GSAP ANIMATIONS
        // ==========================================
        gsap.registerPlugin(ScrollTrigger);

        // ==========================================
        // PARTICLES.JS
        // ==========================================
        particlesJS('particles-js', {
            particles: {
                number: { value: 100, density: { enable: true, value_area: 800 } },
                color: { value: '#00ff88' },
                shape: { type: 'circle' },
                opacity: { value: 0.3, random: true },
                size: { value: 3, random: true },
                line_linked: {
                    enable: true,
                    distance: 150,
                    color: '#00ff88',
                    opacity: 0.2,
                    width: 1
                },
                move: {
                    enable: true,
                    speed: 2,
                    direction: 'none',
                    random: true,
                    out_mode: 'out'
                }
            },
            interactivity: {
                detect_on: 'canvas',
                events: {
                    onhover: { enable: true, mode: 'repulse' },
                    onclick: { enable: true, mode: 'push' }
                }
            }
        });

        // ==========================================
        // 3D SCENE WITH THREE.JS
        // ==========================================
        const canvas = document.getElementById('canvas-3d');
        const scene = new THREE.Scene();
        const camera = new THREE.PerspectiveCamera(75, canvas.offsetWidth / canvas.offsetHeight, 0.1, 1000);
        const renderer = new THREE.WebGLRenderer({ canvas: canvas, alpha: true, antialias: true });
        
        renderer.setSize(canvas.offsetWidth, canvas.offsetHeight);
        renderer.setClearColor(0x000000, 0);

        const geometry = new THREE.TorusKnotGeometry(10, 3, 100, 16);
        const material = new THREE.MeshPhongMaterial({
            color: 0x00ff88,
            wireframe: true,
            transparent: true,
            opacity: 0.8
        });
        const torusKnot = new THREE.Mesh(geometry, material);
        scene.add(torusKnot);

        const light = new THREE.PointLight(0x00ff88, 1, 100);
        light.position.set(10, 10, 10);
        scene.add(light);

        const ambientLight = new THREE.AmbientLight(0x404040);
        scene.add(ambientLight);

        camera.position.z = 30;

        function animate3D() {
            requestAnimationFrame(animate3D);
            torusKnot.rotation.x += 0.005;
            torusKnot.rotation.y += 0.005;
            renderer.render(scene, camera);
        }
        animate3D();

        window.addEventListener('resize', () => {
            camera.aspect = canvas.offsetWidth / canvas.offsetHeight;
            camera.updateProjectionMatrix();
            renderer.setSize(canvas.offsetWidth, canvas.offsetHeight);
        });

        // ==========================================
        // HEADER SCROLL EFFECT
        // ==========================================
        window.addEventListener('scroll', () => {
            const header = document.getElementById('mainHeader');
            if (window.scrollY > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });

        // ==========================================
        // TYPED.JS
        // ==========================================
        new Typed('#typed-subtitle', {
            strings: [
                'Desarrollador en Formación',
                'Apasionado por el Código',
                'Solucionador de Problemas',
                'Estudiante de Programación'
            ],
            typeSpeed: 50,
            backSpeed: 30,
            loop: true,
            backDelay: 2000
        });

        // ==========================================
        // STATS COUNTER
        // ==========================================
        const observeStats = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const counter = entry.target;
                    const target = parseInt(counter.getAttribute('data-count'));
                    const duration = 2000;
                    const increment = target / (duration / 16);
                    let current = 0;

                    const updateCounter = () => {
                        current += increment;
                        if (current < target) {
                            counter.textContent = Math.floor(current);
                            requestAnimationFrame(updateCounter);
                        } else {
                            counter.textContent = target;
                        }
                    };
                    updateCounter();
                    observeStats.unobserve(counter);
                }
            });
        });

        document.querySelectorAll('.stat-number').forEach(counter => {
            observeStats.observe(counter);
        });

        // ==========================================
        // SKILLS PROGRESS ANIMATION
        // ==========================================
        const observeSkills = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const progress = entry.target;
                    const targetWidth = progress.getAttribute('data-progress');
                    setTimeout(() => {
                        progress.style.width = targetWidth + '%';
                    }, 200);
                    observeSkills.unobserve(progress);
                }
            });
        });

        document.querySelectorAll('.skill-progress').forEach(progress => {
            observeSkills.observe(progress);
        });

        // ==========================================
        // FILE UPLOAD HANDLER
        // ==========================================
        const pdfFilesInput = document.getElementById('pdfFiles');
        const selectedFilesDiv = document.getElementById('selectedFiles');

        pdfFilesInput.addEventListener('change', function(e) {
            const files = Array.from(e.target.files);
            if (files.length > 0) {
                selectedFilesDiv.innerHTML = files.map(file => 
                    `<span class="file-tag"><i class="fas fa-file-pdf"></i> ${file.name}</span>`
                ).join('');
            } else {
                selectedFilesDiv.innerHTML = '<span style="color: var(--text-secondary);"><i class="fas fa-info-circle"></i> No hay archivos seleccionados</span>';
            }
        });

        // ==========================================
        // ACTIVITY FORM SUBMISSION
        // ==========================================
        document.getElementById('activityForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const submitBtn = this.querySelector('button[type="submit"]');
            const originalBtnText = submitBtn.innerHTML;
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Subiendo...';
            submitBtn.disabled = true;
            
            try {
                const title = document.getElementById('activityTitle').value;
                const description = document.getElementById('activityDescription').value;
                const files = Array.from(pdfFilesInput.files);
                
                const pdfData = [];
                for (let file of files) {
                    const timestamp = Date.now();
                    const fileName = `${timestamp}_${file.name}`;
                    const storageRef = storage.ref('pdfs/' + fileName);
                    
                    await storageRef.put(file);
                    const downloadURL = await storageRef.getDownloadURL();
                    
                    pdfData.push({
                        name: file.name,
                        url: downloadURL
                    });
                }
                
                const activity = {
                    title: title,
                    description: description,
                    pdfs: pdfData,
                    timestamp: firebase.firestore.FieldValue.serverTimestamp(),
                    date: new Date().toLocaleDateString('es-ES', {
                        year: 'numeric',
                        month: 'long',
                        day: 'numeric',
                        hour: '2-digit',
                        minute: '2-digit'
                    })
                };
                
                await db.collection('activities').add(activity);
                
                this.reset();
                selectedFilesDiv.innerHTML = '<span style="color: var(--text-secondary);"><i class="fas fa-info-circle"></i> No hay archivos seleccionados</span>';
                
                submitBtn.innerHTML = '<i class="fas fa-check"></i> ¡Publicado!';
                setTimeout(() => {
                    submitBtn.innerHTML = originalBtnText;
                    submitBtn.disabled = false;
                }, 2000);
                
                document.getElementById('activitiesList').scrollIntoView({ behavior: 'smooth' });
                
            } catch (error) {
                console.error('Error:', error);
                alert('Error al agregar la actividad. Por favor intenta de nuevo.');
                submitBtn.innerHTML = originalBtnText;
                submitBtn.disabled = false;
            }
        });

        // ==========================================
        // RENDER ACTIVITIES FROM FIREBASE
        // ==========================================
        function renderActivities() {
            const activitiesList = document.getElementById('activitiesList');
            
            db.collection('activities')
                .orderBy('timestamp', 'desc')
                .onSnapshot((snapshot) => {
                    if (snapshot.empty) {
                        activitiesList.innerHTML = `
                            <div class="empty-state">
                                <i class="fas fa-folder-open"></i>
                                <h3>Sin Actividades Registradas</h3>
                                <p>Agrega tu primera actividad usando el formulario</p>
                            </div>
                        `;
                        return;
                    }
                    
                    let htmlContent = '';
                    let delay = 0;
                    
                    snapshot.forEach((doc) => {
                        const activity = doc.data();
                        const activityId = doc.id;
                        
                        htmlContent += `
                            <div class="activity-card" data-aos="fade-up" data-aos-delay="${delay}">
                                <div class="activity-header">
                                    <div>
                                        <h3 class="activity-title">
                                            <i class="fas fa-code-branch"></i> ${activity.title}
                                        </h3>
                                        <p class="activity-date">
                                            <i class="far fa-clock"></i> ${activity.date}
                                        </p>
                                    </div>
                                    <button class="delete-btn" onclick="deleteActivity('${activityId}')">
                                        <i class="fas fa-trash-alt"></i> Eliminar
                                    </button>
                                </div>
                                <p class="activity-description">${activity.description}</p>
                                ${activity.pdfs && activity.pdfs.length > 0 ? `
                                    <div class="pdf-list">
                                        ${activity.pdfs.map(pdf => `
                                            <a href="${pdf.url}" class="pdf-item" target="_blank" rel="noopener noreferrer">
                                                <i class="fas fa-file-pdf"></i>
                                                ${pdf.name}
                                            </a>
                                        `).join('')}
                                    </div>
                                ` : ''}
                            </div>
                        `;
                        delay += 100;
                    });
                    
                    activitiesList.innerHTML = htmlContent;
                    AOS.refresh();
                });
        }

        // ==========================================
        // DELETE ACTIVITY
        // ==========================================
        async function deleteActivity(activityId) {
            if (confirm('⚠️ ¿Confirmar eliminación de esta actividad?')) {
                try {
                    const activityDoc = await db.collection('activities').doc(activityId).get();
                    const activity = activityDoc.data();
                    
                    if (activity.pdfs && activity.pdfs.length > 0) {
                        for (let pdf of activity.pdfs) {
                            try {
                                const pdfRef = storage.refFromURL(pdf.url);
                                await pdfRef.delete();
                            } catch (error) {
                                console.error('Error al eliminar PDF:', error);
                            }
                        }
                    }
                    
                    await db.collection('activities').doc(activityId).delete();
                    
                } catch (error) {
                    console.error('Error:', error);
                    alert('Error al eliminar la actividad.');
                }
            }
        }

        // ==========================================
        // SMOOTH SCROLL
        // ==========================================
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        // ==========================================
        // INITIALIZE
        // ==========================================
        renderActivities();
    </script>
</body>
</html>
